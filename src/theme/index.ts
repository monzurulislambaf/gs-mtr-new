/**
 * Design system barrel export.
 *
 * Import tokens from here instead of individual files:
 *   import { spacing, typography, radius, colors } from '@/theme';
 */
export { colors, useColors, type ColorTokens } from './colors';
export { spacing, type SpacingToken } from './spacing';
export { typography, type TypographyToken } from './typography';
export { radius, type RadiusToken } from './radius';
export { shadows, type ShadowToken } from './shadows';
export { motion, spring } from './motion';
